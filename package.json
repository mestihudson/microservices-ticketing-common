{
  "name": "@mestihudson-ticketing/common",
  "version": "1.0.5",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "prebuild": "del ./build/*",
    "build": "tsc",
    "dev": "nodemon --exec 'yarn build' --watch 'src/*'",
    "pub": "git diff --exit-code --quiet && yarn version --patch && yarn build && yarn publish --non-interactive"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "nodemon": "^2.0.15",
    "typescript": "^4.4.4"
  }
}
