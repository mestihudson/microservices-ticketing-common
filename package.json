{
  "name": "@mestihudson-ticketing/common",
  "version": "1.0.1",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "prebuild": "del ./build/*",
    "build": "tsc",
    "pub": "git diff --exit-code --quiet && npm version patch && yarn build yarn publish"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "typescript": "^4.4.4"
  }
}
